<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SafeIn_Mobile.Views.LoginPage"             
             Shell.FlyoutBehavior="Disabled"
             Shell.NavBarIsVisible="False" 
             xmlns:viewmodels="clr-namespace:SafeIn_Mobile.ViewModels">

    <!--<ContentPage.BindingContext>
        <viewmodels:LoginViewModel/>
    </ContentPage.BindingContext>-->


    <ContentPage.Content>
        <StackLayout Padding="40" VerticalOptions="Center"
                     HorizontalOptions="FillAndExpand">
            <Label Text="Email:"/>
            <Entry Text="{Binding Email, Mode=TwoWay}" Placeholder="Username"/>
            <Label Text="Password:"/>
            <Entry Text="{Binding Password}" IsPassword="True" Placeholder="Password"/>
            <Button Text="Login" Command ="{Binding LoginCommand}"/>

            <!--<Button Text="Go to User Page" Command="{Binding NavigateToUserPageCommand}" />-->
        </StackLayout>
    </ContentPage.Content>
</ContentPage>